//
//  weighted_set.cpp
//  CBM
//
//

#include "weighted_set.hpp"

/*
template<typename T>
weighted_set<T>::weighted_set() {
    elems = {};
}

template<typename T>
void weighted_set<T>::addNbr( T* newElem, double weight ) {
    elems.emplace_back( newElem, weight );
}

template<typename T>
weightedElem<T>& weighted_set<T>::operator[]( const uint &index ) {
    return elems[index];
}
*/
